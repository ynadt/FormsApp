export const baseUrl = import.meta.env.VITE_API_URL;
export const drawerWidth = `calc(56px + 1px)`;
export const appBarHeight = '64px';
export const MAX_FILE_SIZE_UPLOAD = 50 * 1024 * 1024;
export const MAX_FILE_SIZE_MB = 50;
